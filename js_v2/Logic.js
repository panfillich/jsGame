//Формируем логику игры, (тригеры и нужная послед-ность анимаций)function Logic(setting, map){	 this._setting = setting;	 this._map = map;	 this.i = 0;}//Начало игрыLogic.prototype.start = function(){	motion_callback.push(function(){		var setting = this._setting;		this._setting.context.clearRect(0, 0, setting.map.width, setting.map.height);				this._map.nodes.forEach(function(node,i,nodes){			setting.context.drawImage(setting.image_store[0].img, node.pos.drow_point.x, node.pos.drow_point.y, setting.hex.size.x, setting.hex.size.y );		});		}.bind(this));}